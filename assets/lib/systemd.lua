#!/bin/lua

local screen = {
    title = "Services",
    back = { root = os.exit },
    button = { label = "" }
}
local function handler()
    
end
local function main()
    
end

os.setproc("name", "systemd")
main()