[ Config ]

name=Tab
version=1.0
description=Auto Syntax

api.version=1.13
api.error=execute echo [Tab] Requires API 1.13; log add error Tab requires OpenTTY 1.13;

include=forge

config=execute x11 item tab;

[ COMMAND ]

item.label=TAB
item.cmd=if ($PROMPT) execute if ($PROMPT == ex) buff exec; if ($PROMPT == execu) buff execute; if ($PROMPT == execut) buff; if ($PROMPT == x) buff x11; if ($PROMPT == canvas) buff x11 canvas; if ($PROMPT == quest) buff x11 quest; if ($PROMPT == make) buff x11 make; if ($PROMPT == list) buff x11 list;

# if ($PROMPT == )
# if ($PROMPT == )