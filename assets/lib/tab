[ Config ]

name=Tab
version=1.0
description=Auto Syntax

api.version=1.13
api.error=execute echo [Tab] Requires API 1.13; log add error Tab requires OpenTTY 1.13;

include=forge

config=execute x11 item tab;

[ COMMAND ]

item.label=TAB
item.cmd=if ($PROMPT) execute if ($PROMPT startswith exe) buff execute; 

# if ($PROMPT == )
# if ($PROMPT == )