#!/bin/lua

local function shell(args)
    local argument = {}
    for i = 1,#args - 1 then
        argument[i] = args[i]
    end
    argument = table.concat(argument)
    
    if args[1] == "" then
    elseif io.open("/bin/" .. args[1]) then
        local res = io.popen("/bin/" .. args[1], table.concat(), false, io.stdout)
        return res.status
    elseif mainCommand == "env" then
    elseif mainCommand == "echo" then
        print(argument)
    elseif mainCommand == "clear" then
        io.write("", io.stdout)
    else
        print(mainCommand .. ": not found")
        return 127
    end

    return 0
end

os.exit(shell(arg))