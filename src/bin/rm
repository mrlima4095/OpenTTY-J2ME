#!/bin/lua

if arg[1] then
    for i = 1, #arg - 1 do
        local status os.remove(os.join(arg[1]))

        if status == 1 then
            
        end
    end
else
    print("rm: usage: rm [file]")
    os.exit(2)
end