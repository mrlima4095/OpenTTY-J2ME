package javax.wireless.messaging;

import javax.microedition.io.Connection;

/** Minimal stub so OpenTTY's unused `import javax.wireless.messaging.*` resolves. */
public interface MessageConnection extends Connection {
}